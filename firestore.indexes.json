{
  "indexes": [
    {
      "comment": "listProjects() filters on ownerUid and orders by updatedAt. Firestore requires a composite index for equality + orderBy on different fields; without it the projects list throws FAILED_PRECONDITION at runtime.",
      "collectionGroup": "projects",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "ownerUid", "order": "ASCENDING" },
        { "fieldPath": "updatedAt", "order": "DESCENDING" }
      ]
    }
  ],
  "fieldOverrides": []
}
